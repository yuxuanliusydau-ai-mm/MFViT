[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mfvit-engineering"
version = "0.1.0"
description = "Engineering implementation of MF-ViT (fractional differential operators + ViT) for desnowing"
requires-python = ">=3.9"
